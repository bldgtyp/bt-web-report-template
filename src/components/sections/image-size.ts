/** Intrinsic size of the scraped window-study renders, shared by the image
 * sections so web and print emit identical layout hints. */
export const SECTION_IMAGE_SIZE = { width: 1200, height: 927 } as const;
